pipeline {
  agent {
    node {
      label '6.3'
    }

  }
  stages {
    stage('Build') {
      steps {
        sh 'sh \'npm --version\''
      }
    }

  }
  environment {
    PATH = 'C:\\Program Files\\Git\\usr\\bin'
  }
}